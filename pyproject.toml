[project]
name = "arcaea-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "AnasElgamed8", email = "anashossamhafez@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "marimo>=0.24.0",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]

[project.scripts]
arcaea-scraper = "arcaea_scraper:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
